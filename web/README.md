# BeyondChats Web

React UI that fetches articles from the Laravel API and displays original and updated versions.

## Setup

```bash
npm install
```

Create `.env` from `.env.example` and update `VITE_API_BASE_URL` if needed.

## Run

```bash
npm run dev
```
